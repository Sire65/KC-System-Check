import "jsr:@supabase/functions-js/edge-runtime.d.ts";
const CORS={"Access-Control-Allow-Origin":"*","Access-Control-Allow-Headers":"authorization, x-client-info, apikey, content-type","Access-Control-Allow-Methods":"GET, OPTIONS","Content-Type":"application/json; charset=utf-8","Cache-Control":"no-store"};
const FUTURE_URL="https://iddudrxuihdodnvejxcp.supabase.co",FUTURE_KEY="sb_publishable_DWLycZijZEBvakXVncI5IQ_38LZCQxW",GITHUB_REPO="https://api.github.com/repos/Sire65/KC-System-Check",FREE_DB_BYTES=500*1024*1024,enc=new TextEncoder();
const pct=(n:number,d:number)=>d>0?Math.round(n/d*1000)/10:null,mb=(n:number)=>Math.round(n/1024/1024*10)/10;
async function timed(url:string,init:RequestInit={}){const s=Date.now(),r=await fetch(url,{...init,cache:"no-store"});return{r,ms:Date.now()-s}}
function mirrorHealth(s:any){const m=s?.mirror??{},age=m.finished_at?Math.round((Date.now()-new Date(m.finished_at).getTime())/60000):null,mis=Number(m.mismatch_count??0),non=Number(s?.non_ok_24h??0);let status="healthy",health=100;if(age===null||age>180){status="warning";health=72}if(mis>0||m.status!=="ok"){status="critical";health=35}else if(non>0){status="warning";health=Math.min(health,88)}return{status,health,age_min:age}}
const worst=(a:any[])=>a.some(x=>x.status==="critical")?"critical":a.some(x=>x.status==="warning")?"warning":"healthy";
const health=(a:any[])=>{const x=a.filter(v=>["healthy","warning","critical"].includes(v.status)&&Number.isFinite(v.health));return x.length?Math.round(x.reduce((s,v)=>s+v.health,0)/x.length):0};
async function history(url:string){const k=Deno.env.get("SUPABASE_SERVICE_ROLE_KEY")!,r=await fetch(`${url}/rest/v1/kc_system_check_history?select=checked_at,trigger_type,overall_status,health,duration_ms,request_count,response_bytes,results&order=checked_at.desc&limit=40`,{headers:{apikey:k,Authorization:`Bearer ${k}`}});return r.ok?await r.json():[]}
async function usage(url:string){const rows=await history(url),from=Date.now()-31*86400000,m=rows.filter((x:any)=>Date.parse(x.checked_at)>=from);return{runs_31d:m.length,auto_31d:m.filter((x:any)=>x.trigger_type==="auto").length,manual_31d:m.filter((x:any)=>x.trigger_type!=="auto").length,requests_31d:m.reduce((n:number,x:any)=>n+Number(x.request_count||0),0),response_bytes_31d:m.reduce((n:number,x:any)=>n+Number(x.response_bytes||0),0)}}
Deno.serve(async(req:Request)=>{if(req.method==="OPTIONS")return new Response(null,{status:204,headers:CORS});if(req.method!=="GET")return new Response(JSON.stringify({error:"method_not_allowed"}),{status:405,headers:CORS});const started=Date.now(),u=new URL(req.url),own=Deno.env.get("SUPABASE_URL")!,service=Deno.env.get("SUPABASE_SERVICE_ROLE_KEY")!;try{if(u.searchParams.get("history")==="1")return new Response(JSON.stringify({version:"0.3.0-live",history:await history(own),usage:await usage(own)}),{headers:CORS});let requests=0;const sr=await timed(`${own}/rest/v1/rpc/kc_system_check_snapshot`,{method:"POST",headers:{apikey:service,Authorization:`Bearer ${service}`,"Content-Type":"application/json"},body:"{}"});requests++;if(!sr.r.ok)throw new Error(`snapshot_http_${sr.r.status}`);const snap=await sr.r.json(),mh=mirrorHealth(snap),coreBytes=Number(snap?.database_bytes??0);const fr=await timed(`${FUTURE_URL}/rest/v1/rpc/kc_system_check_public_snapshot`,{method:"POST",headers:{apikey:FUTURE_KEY,Authorization:`Bearer ${FUTURE_KEY}`,"Content-Type":"application/json"},body:"{}"}).catch(()=>null);requests++;let fd:any={};if(fr?.r?.ok){try{fd=await fr.r.json()}catch{}}const futureBytes=Number(fd?.database_bytes??0);const gh=await timed(GITHUB_REPO,{headers:{Accept:"application/vnd.github+json","User-Agent":"KC-System-Check"}}).catch(()=>null);requests++;let gd:any={};if(gh?.r?.ok){try{gd=await gh.r.json()}catch{}}const nu=Deno.env.get("NEON_HEALTH_URL")||"";let neon:any=null;if(nu){neon=await timed(nu,{headers:Deno.env.get("NEON_HEALTH_TOKEN")?{Authorization:`Bearer ${Deno.env.get("NEON_HEALTH_TOKEN")}`}:{}}).catch(()=>null);requests++}const bu=Deno.env.get("B2_STATUS_URL")||"";let b2:any=null;if(bu){b2=await timed(bu,{headers:Deno.env.get("B2_STATUS_TOKEN")?{Authorization:`Bearer ${Deno.env.get("B2_STATUS_TOKEN")}`}:{}}).catch(()=>null);requests++}const cu=pct(coreBytes,FREE_DB_BYTES),fu=pct(futureBytes,FREE_DB_BYTES);const all:any[]=[{id:"kc_core",name:"KC Core · Supabase",kind:"database",status:sr.ms>3000?"warning":"healthy",health:sr.ms>3000?82:sr.ms>1500?90:100,latency:sr.ms,usage:cu,capacityLabel:`${mb(coreBytes)} / 500 MB`,detail:sr.ms>3000?"Erreichbar, aber langsam":"KC Core erreichbar",metrics:{database_bytes:coreBytes,free_tier_database_bytes:FREE_DB_BYTES,usage_percent:cu,latency_ms:sr.ms}},{id:"future_academy",name:"Future Academy · Supabase",kind:"database",status:fr?.r?.ok?(fr.ms>3000?"warning":"healthy"):"warning",health:fr?.r?.ok?(fr.ms>3000?82:100):75,latency:fr?.ms??null,usage:fr?.r?.ok?fu:null,capacityLabel:fr?.r?.ok?`${mb(futureBytes)} / 500 MB`:"Kapazität nicht verfügbar",detail:fr?.r?.ok?"Future Academy erreichbar":"Future Academy nicht erreichbar",metrics:{database_bytes:futureBytes||null,public_tables:fd?.public_tables??null,http_status:fr?.r?.status??null,usage_percent:fr?.r?.ok?fu:null,latency_ms:fr?.ms??null}},{id:"mirror",name:"Spiegelung",kind:"replication",status:mh.status,health:mh.health,latency:null,usage:null,capacityLabel:`${snap?.runs_24h??0} Läufe / 24 h`,detail:mh.status==="healthy"?`${snap?.mirror?.mismatch_count??0} Abweichungen · letzter Lauf vor ${mh.age_min??"?"} min`:mh.status==="warning"?"Spiegelung mit Warnhinweis":"Aktive Spiegelabweichung oder Fehler",metrics:{...snap?.mirror,age_min:mh.age_min,runs_24h:snap?.runs_24h,non_ok_24h:snap?.non_ok_24h,mismatches_24h:snap?.mismatches_24h}},{id:"neon",name:"Neon",kind:"database",status:neon?(neon.r.ok?"healthy":"warning"):"unknown",health:neon?(neon.r.ok?100:70):null,latency:neon?.ms??null,usage:null,capacityLabel:neon?"Direkter Read-only-Healthcheck":"Direkter Adapter vorbereitet",detail:neon?(neon.r.ok?"Neon direkt erreichbar":"Neon-Direktcheck auffällig"):"Keine sichere Neon-Direkt-URL hinterlegt; Mirror-Ziel wird separat überwacht",metrics:{direct_check:!!neon,http_status:neon?.r?.status??null,mirror_target_rows:snap?.mirror?.target_rows??null}},{id:"github",name:"GitHub",kind:"service",status:gh?.r?.ok?"healthy":"warning",health:gh?.r?.ok?100:78,latency:gh?.ms??null,usage:null,capacityLabel:gh?.r?.ok?`Repo ${gd?.size??0} KB`:"GitHub API nicht erreichbar",detail:gh?.r?.ok?"Repository öffentlich erreichbar":"GitHub-Prüfung fehlgeschlagen",metrics:{http_status:gh?.r?.status??null,repo_size_kb:gd?.size??null,default_branch:gd?.default_branch??null}},{id:"b2",name:"Backblaze B2",kind:"storage",status:b2?(b2.r.ok?"healthy":"warning"):"disabled",health:b2?(b2.r.ok?100:70):null,latency:b2?.ms??null,usage:null,capacityLabel:b2?"Read-only-Status aktiv":"Noch nicht sicher angebunden",detail:b2?(b2.r.ok?"B2 Read-only erreichbar":"B2 Read-only auffällig"):"Kein Test-Upload; Adapter bleibt bis zur Read-only-Konfiguration deaktiviert",metrics:{direct_check:!!b2,http_status:b2?.r?.status??null}}];const aliases:any={supabase:["kc_core","future_academy"]},raw=u.searchParams.get("systems")?.split(",").filter(Boolean)||[],wanted=[...new Set(raw.flatMap(x=>aliases[x]||[x]))],results=wanted.length?all.filter(x=>wanted.includes(x.id)):all,status=worst(results),h=health(results),coverage=Math.round(results.filter(x=>["healthy","warning","critical"].includes(x.status)).length/Math.max(1,results.length)*100),duration=Date.now()-started,payload:any={version:"0.3.0-live",status,health:h,coverage,checkedAt:new Date().toISOString(),duration_ms:duration,capacity:{kc_core_database_bytes:coreBytes,future_academy_database_bytes:futureBytes||null,free_database_bytes:FREE_DB_BYTES},resource_usage:{request_count:requests,estimated:true},results};const bytes=enc.encode(JSON.stringify(payload)).byteLength;payload.resource_usage.response_bytes=bytes;if(u.searchParams.get("record")!=="0"){const trigger=["auto","selected"].includes(u.searchParams.get("trigger")||"")?u.searchParams.get("trigger"):"manual";await fetch(`${own}/rest/v1/kc_system_check_history`,{method:"POST",headers:{apikey:service,Authorization:`Bearer ${service}`,"Content-Type":"application/json","Prefer":"return=minimal"},body:JSON.stringify({trigger_type:trigger,overall_status:status,health:h,duration_ms:duration,request_count:requests,response_bytes:bytes,results})}).catch(()=>null)}return new Response(JSON.stringify(payload),{headers:CORS})}catch(e){return new Response(JSON.stringify({version:"0.3.0-live",status:"critical",health:0,checkedAt:new Date().toISOString(),error:String((e as Error)?.message||e)}),{status:500,headers:CORS})}});
