function setup(canvas){const c=canvas.getContext("2d");c.clearRect(0,0,canvas.width,canvas.height);return c}export function drawLine(canvas,series){const c=setup(canvas),w=canvas.width,h=canvas.height,p=34;c.strokeStyle="#273552";c.lineWidth=1;for(let i=0;i<5;i++){let y=p+(h-2*p)*i/4;c.beginPath();c.moveTo(p,y);c.lineTo(w-p,y);c.stroke()}if(!series.length)return;const max=Math.max(100,...series.map(x=>x.value))*1.15;c.beginPath();series.forEach((d,i)=>{const x=p+(w-2*p)*(i/Math.max(1,series.length-1)),y=h-p-(h-2*p)*(d.value/max);i?c.lineTo(x,y):c.moveTo(x,y)});c.strokeStyle="#5aa7ff";c.lineWidth=4;c.stroke()}export function drawBars(canvas,values){const c=setup(canvas),w=canvas.width,h=canvas.height,p=34;if(!values.length)return;const bw=(w-2*p)/values.length*.66;values.forEach((v,i)=>{const x=p+(w-2*p)*(i/values.length)+(w-2*p)/values.length*.17,bh=(h-2*p)*(v.health/100);c.fillStyle=v.health>=90?"#2ecc71":v.health>=70?"#f3c34d":"#ef5a5a";c.fillRect(x,h-p-bh,bw,bh)})}
