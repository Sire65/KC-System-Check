function setup(canvas){const c=canvas.getContext("2d");c.clearRect(0,0,canvas.width,canvas.height);return c}
function shortLabel(v){return String(v||'').replace(' · Supabase','').replace('Spiegelung · ','').replace('Future Academy','Academy').slice(0,18)}
export function drawLine(canvas,series){const c=setup(canvas),w=canvas.width,h=canvas.height,desktop=(canvas.clientWidth||0)>=520,p=desktop?54:34;c.strokeStyle="#273552";c.lineWidth=1;const max=Math.max(100,...series.map(x=>x.value))*1.15;for(let i=0;i<5;i++){let y=p+(h-2*p)*i/4;c.beginPath();c.moveTo(p,y);c.lineTo(w-p,y);c.stroke();if(desktop){const val=Math.round(max*(1-i/4));c.fillStyle="#9ba7bd";c.font="12px system-ui,Arial";c.textAlign="right";c.textBaseline="middle";c.fillText(`${val} ms`,p-8,y)}}if(!series.length)return;c.beginPath();series.forEach((d,i)=>{const x=p+(w-2*p)*(i/Math.max(1,series.length-1)),y=h-p-(h-2*p)*(d.value/max);i?c.lineTo(x,y):c.moveTo(x,y)});c.strokeStyle="#5aa7ff";c.lineWidth=4;c.stroke();if(desktop){series.forEach((d,i)=>{const x=p+(w-2*p)*(i/Math.max(1,series.length-1)),y=h-p-(h-2*p)*(d.value/max);c.fillStyle="#5aa7ff";c.beginPath();c.arc(x,y,4,0,Math.PI*2);c.fill();c.fillStyle="#f5f7fb";c.font="12px system-ui,Arial";c.textAlign="center";c.textBaseline="bottom";c.fillText(`${Math.round(d.value)} ms`,x,y-8);c.fillStyle="#9ba7bd";c.textBaseline="top";c.fillText(shortLabel(d.label),x,h-p+10)})}}
export function drawBars(canvas,values){const c=setup(canvas),w=canvas.width,h=canvas.height,p=34;if(!values.length)return;const bw=(w-2*p)/values.length*.66;values.forEach((v,i)=>{const x=p+(w-2*p)*(i/values.length)+(w-2*p)/values.length*.17,bh=(h-2*p)*(v.health/100);c.fillStyle=v.health>=90?"#2ecc71":v.health>=70?"#f3c34d":"#ef5a5a";c.fillRect(x,h-p-bh,bw,bh)})}
